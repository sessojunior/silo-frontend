import React from 'react'
import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const App = () => {
	return (
		<SafeAreaView>
			<Text className='text-blue-600 font-bold text-3xl '>Silo</Text>
		</SafeAreaView>
	)
}

export default App
